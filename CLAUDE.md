# CLAUDE.md

The project instructions for this repo live in **AGENTS.md** (the tool-agnostic standard). Claude Code reads `CLAUDE.md`, not `AGENTS.md`, so this file imports it to keep a single source of truth with no duplication:

@AGENTS.md